#include "main.h"
/**
 *_abs - calculate the absulute in number
 *@i; is number calculate the absluto in number
 *Return: absolute value is number
 */
int _abs(int i)
{
	int a;

	i = _abs(i);

	return (i); 
}
