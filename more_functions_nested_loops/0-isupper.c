#include "main.h"
#include <stdio.h>

/**
 * _isuper  - check character.
 * @c: check letter
 * Retur: Value letter
 */
int _isupper(int c)
{

	if (c >=65 && c<=90 )
		return(1);
	else
	return(0);
}

