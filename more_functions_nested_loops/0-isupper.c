#include "main.h"
#include <stdio.h>

/**
 * _isuper  - check character.
 * @c: check letter
 * Retur: Value letter
 */
int _isupper(int c)
{
	int c;

	if (_isupper (int c))
		return(1);
	else
	return(0);
}

